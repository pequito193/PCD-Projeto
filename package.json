{
  "name": "java-runner",
  "version": "1.0.0",
  "scripts": {
    "server": "java -cp \"lib/gson-2.10.1.jar;src\" Server.Server",
    "client": "node -e \"const [team, player] = process.argv.slice(1); const { execSync } = require('child_process'); execSync(`java -cp \\\"lib/gson-2.10.1.jar;src\\\" client.ClientGUI localhost 12345 Jogo1 ${team} ${player}`, { stdio: 'inherit' });\""
  }
}
